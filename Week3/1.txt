如果第一次输入的字符串 通过去除空格和占位符后 整理后的格式为a~b，
就从a~b中取出a和b 同时满足0 <= a <= b <= 1114111时，
    计算出处于a和b之间有多少被命名的Unicode的百分比
    当结果大于0小于等于100
        分别输出不同的提示（全部或者百分比）
        字符串输入提示
        第二次输入一个字符串
            使用输入的字符串进行匹配，匹配相关的在ab范围间的UNicode
            匹配有则全部输出
            匹配没有，输出没有这个字符串，结束
    当结果等于0
        输出没有，同时结束
输出错误输入，同时返回重新输入
