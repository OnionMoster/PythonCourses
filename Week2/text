第一：L数组
第二：pattern 根据'+''-'判断前后大小关系：
    '+'：前小于后
    '-'：前大于后
（不根据第三条变换顺序）
第三：from_start ture从左到右 false从右到左
第四：如果不满足第二条，pop()掉，直到满足第二条的元素出现

结果1：List (删除的位置：元素)
结果2：剩下的L


首先看from_start的真假，如果是真，就从左到右看数组，然后看pattern，按照+--就是第一个数变大到第二个数，然后第二个数变小到第三个数，然后再变小到第四个数，一直循环，同时将不符合规则的数排除，直到能够符合规则